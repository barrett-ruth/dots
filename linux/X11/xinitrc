#!/bin/sh
if [ -d /etc/X11/xinit/xinitrc.d ]; then
  for f in /etc/X11/xinit/xinitrc.d/*; do
    [ -x "$f" ] && . "$f"
  done
  unset f
fi

smon
sxs
(
  sbg ~/img/wp/two/calvin.jpg
  skb
) &
unclutter 10 &
sclip &
(st -e mux) &
while true; do
  dwmblocks &
  dwm
  continue || exit
done
