#!/bin/sh

proj="$PWD"

case "$(basename "$proj")" in
theCourseForum2)
  # docker compose up --build
  cmd='docker compose up'
  ;;
atlas)
  cmd='pnpm run dev'
  ;;
*)
  test -f Cargo.toml && cmd='cargo build'
  ;;
esac

echo " > $cmd" | sed "s|$HOME|~|g"
eval "$cmd"
