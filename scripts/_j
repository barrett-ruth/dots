#!/usr/bin/env zsh

case "$1" in
    -l)
        for k v in "${(@kv)dirs}"; do
            echo -n "[$k]=$v | " | sed "s|$HOME|~|g"
        done;;
    -)
        cd - >/dev/null && return
        ;;
    *)
        dir=$dirs[$1];;
esac

cd "$dir"
