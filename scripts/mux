#!/bin/sh

attach_or_restore() {
    [ "$(pgrep tmux)" ] || cmd='start\; run ~/.config/tmux/tmux-resurrect/scripts/restore.sh\; '

    eval "tmux $cmd"a 2>/dev/null || tmux
}

[ "$1" ] || attach_or_restore

case "$1" in
bar)
    bar="$(tmux ls -F '#S' | nl -w1 -v0 -s ':' | paste -sd '|' | sed "s/|/#[fg=white] │ #[fg=#928374]/g;s/$2/$2*/")"
    echo "#[fg=#928374]$bar"
    ;;
git)
    tmux neww -c '#{pane_current_path}' -n git 'nvim -c "Git|only"'
    ;;
run)
    [ "$(tmux lsw -F '#W' | rg run)" ] || tmux neww -c '#{pane_current_path}' -n run

    tmux send -t run 'C-c' 'enter' "cd $2" 'enter' 'C-l' 'mux-run' 'enter'
    ;;
switch)
    mux-switch "$2"
    ;;
esac
