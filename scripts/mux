#!/bin/sh

attach_or_restore() {
    [ "$(pgrep tmux)" ] || cmd='start\; run ~/.config/tmux/tmux-resurrect/scripts/restore.sh\; '

    eval "tmux $cmd"a
}

[ "$1" ] || attach_or_restore

case "$1" in
bar)
    bar="$(tmux ls -F '#S' | nl -w1 -v0 -s ':' | paste -sd '|' | sed "s/|/#[fg=white] │ #[fg=#928374]/g;s/$2/$2*/")"
    echo "#[fg=#928374]$bar "
    ;;
run)
    [ "$(tmux lsw -F '#W' | rg run)" ] || tmux neww -c '#{pane_current_path}' -n run
    tmux send 'C-l' 'run "$(pwd)"' 'enter'
    ;;
*)
    session_name="$1"
    session="$(tmux ls -F '#S' | tail -n "+$((session_name + 1))" | head -1)"
    tmux switch -t "$session"
    ;;
esac
