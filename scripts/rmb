#!/bin/sh

[ "$1" ] || exit 1

if [ -d "$1" ]; then
    dir="$1"
    shift
else
    dir='.'
fi

args="$*"
for e in .[!.]* *; do
    case "$args" in
    *"$e"*) continue ;;
    esac
    rm -rf "${dir:?}/$e"
done
