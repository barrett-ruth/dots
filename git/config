[user]
    name = Barrett Ruth
    email = br@barrettruth.com
    username = barrett-ruth
[init]
    defaultBranch = main
[core]
    editor = nvim
    whitespace = fix,-indent-with-non-tab,trailing-space,cr-at-eol
[color]
    ui = auto
[diff]
    tool = nvimdiff
[difftool "nvimdiff"]
    cmd = "nvim -d \"$LOCAL\" \"$REMOTE\""
[alias]
    a = add
    b = branch
    c = commit
    cap = "!cap() { git add . && git commit -m \"$*\" && git push; }; cap"
    cf = config
    ch = checkout
    cl = clone
    d = diff
    dt = difftool
    f = fetch
    i = init
    lg = log --oneline --graph --decorate
    mg = merge
    p = pull
    pu = push
    r = remote
    rs = restore
    rt = reset
    s = status
    sw = switch
    wt = worktree
