require('diffview').setup {
    enhanced_diff_hl = true,
    use_icons = false,
    icons = {
        folder_closed = '',
        folder_open = ''
    },
    signs = {
        fold_closed = '',
        fold_open = '',
        done = ''
    }
}
